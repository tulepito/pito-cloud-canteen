import Modal from '@components/Modal/Modal';
import React from 'react';

const ResultDetailModal: React.FC = () => {
  return (
    <Modal isOpen={false} handleClose={() => null}>
      <div>ResultDetailModal</div>
    </Modal>
  );
};

export default ResultDetailModal;
