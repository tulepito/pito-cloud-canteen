// Listing types
const FOOD = 'food';
const RESTAURANT = 'restaurant';
const ORDER = 'order';
const SUB_ORDER = 'sub-order';

const LISTING_TYPE = {
  FOOD,
  RESTAURANT,
  ORDER,
  SUB_ORDER,
};

export { LISTING_TYPE };
