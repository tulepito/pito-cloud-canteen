// HTTP methos
const GET = 'GET';
const POST = 'POST';
const PUT = 'PUT';
const DELETE = 'DELETE';

const HTTP_METHODS = {
  GET,
  POST,
  PUT,
  DELETE,
};

// Listing types
const FOOD = 'food';
const RESTAURANT = 'restaurant';
const ORDER = 'order';
const SUB_ORDER = 'sub-order';

const LISTING_TYPE = {
  FOOD,
  RESTAURANT,
  ORDER,
  SUB_ORDER,
};

export { HTTP_METHODS, LISTING_TYPE };
